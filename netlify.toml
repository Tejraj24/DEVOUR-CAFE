[build]
  base = "frontend"
  command = "npm run build"
  publish = "frontend/dist"

  [build.environment]
  NODE_VERSION = "20"

# SPA redirect so client-side routes work on refresh
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
